#include <bits/stdc++.h>

using namespace std;
double r, A, P, pi = M_PI ;
int main()
{
  cin >> r ;
   A = pi * r * r ;
   P = 2 * pi * r ;
   cout << fixed << setprecision(6) << A << ' ' << P ;

   return 0;
}
